When your response has distinct sections (reasoning, answer, citations), wrap each
in matching XML-style tags so downstream parsers can extract them reliably. Example:

<reasoning>...</reasoning>
<answer>...</answer>
